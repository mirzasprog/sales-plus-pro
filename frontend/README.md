# Retail Positions Frontend

Angular SPA for managing additional sales positions.

## Setup

1. Install Node.js 18+.
2. Install dependencies:

```bash
npm install
```

3. Start development server:

```bash
npm run start
```

App is served at `http://localhost:4200`.
